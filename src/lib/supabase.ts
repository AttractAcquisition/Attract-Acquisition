// Updated Client and Prospect Interfaces

interface Client {
    // ... other properties
    meta_pixel_id: string; // New property
}

interface Prospect {
    // ... other properties
    address: string; // New property
}

// Added missing interfaces
interface Template {
    // Define the structure
}

interface Sprint {
    // Define the structure
}

interface CapitalRow {
    // Define the structure
}

interface PortalClient {
    // Define the structure
}

interface PortalSprint {
    // Define the structure
}

interface DailyLog {
    // Define the structure
}

interface OutreachMsg {
    // Define the structure
}